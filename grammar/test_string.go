package main

import (
	"fmt"
	"strings"
)

func main() {
	ret := strings.Contains("新手宝宝注意查收一下", "100")
	fmt.Println(ret)

	/*
		str := " 123 "
		ret := strings.TrimSpace(str)
		fmt.Println(ret)
	*/
}
